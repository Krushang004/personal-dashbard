import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krushang.dashboard',
  appName: 'personal-life-dashboard',
  webDir: 'out'
};

export default config;
